(:myGo
  (:myGo
    0 0
    rax <- :label
    rax <- 11
    r10 <- rax
    rbx <- 9
    rax <<= 2
    mem r10 16 <- 5
    rbx <- mem r14 8
    r11++
    r12--
    rdi <- 10 <= 11
    rdi <- 10 < 9
    rdi <- rax <= 10
    rdi <- rax < 10
    rdi <- rax = 10
    rsi <- 10 = rax
    return
  )
  (:anotherFunc
    0 0
    return
  )
)
