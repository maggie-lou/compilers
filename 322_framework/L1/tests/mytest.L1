(:myGo
  (:myGo
    0 0
    rax <- :label
    rax <- 11
    r10 <- rax
    rbx <- 9
    rax <<= 2
    mem r10 16 <- 5
    rbx <- mem r14 8
    r11++
    r12--
    rdi <- 10 <= 11
    rdi <- 10 < 9
    rdi <- rax <= 10
    rdi <- rax < 10
    rdi <- rax = 10
    rsi <- 10 = rax
    goto :label
    :label
    mem rsp -8 <- :myLabel
    cjump rax <= rdi :yes :no
    cjump 8 < rdi :yes :no
    cjump 3 < 1 :true :false
    cjump rax = rdi :yes
    cjump 1 <= 3 :true
    cjump 3 <= 1 :true
    rax @ rdi rsi 4
    return
  )
  (:anotherFunc
    0 0
    return
  )
)
