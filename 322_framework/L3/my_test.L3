define :myEqual (p1,   p2){
  var1 <- :label1
  var1 <-  var2
  var1 <- 8
  var1 <- -17

  var <- var2 + 6
  var <- var2 - 6
  var <- var2 * 6
  var <- var2 & var3
  var <- 8 << var4
  var <- var2 >> 6

  var <- var5  < 9
  var <- var5  <= 9
  var <- var5  = 9
  var <- var5  > var7
  var <- 0  >= 9

  var <- load var9

  store var00 <- :ll
  store var00 <- var444
  store var00 <- 9

  br :lll

  :mylabel

  br varo :label1 :lab3

  return

  return varvar
  return -7

  call print ( 6, 8,third)
  call allocate ( 6, 4)
  call array-error (third)
  call myfunc(a1,6,  -9,a2)

  ret <- call print ( -6, 8,third)
  ret3 <- call allocate ( 6, 4)
  ret4 <- call array-error (third)
  ret2 <- call myfunc(a1,6,  -9,a2)
}
